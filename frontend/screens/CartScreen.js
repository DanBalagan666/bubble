import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';
export default function CartScreen(){ return (<SafeAreaView style={{flex:1,backgroundColor:'#07060a',alignItems:'center',justifyContent:'center'}}><Text style={{color:'#fff'}}>Корзина (пока демо)</Text></SafeAreaView>); }
